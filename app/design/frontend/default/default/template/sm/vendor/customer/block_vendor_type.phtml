<?php
/**
 * add infomation of vedor when customer register
 */
?>
<div class="fieldset">
    <h2 class="legend">Customer Type</h2>
    <ul class="form-list">
        <li class="fields">
            <div class="field">
                <label for="type" class="required"><em>*</em>
                    Customer type
                </label>

                <div class="input-box">
                    <select name="customer_type" class="input-text required-entry validate­select" id="select-customer-type">
                        <option value="">--choose customer type--</option>
                        <option value="normal">Normal</option>
                        <option value="vendor">Vendor</option>
                    </select>
                </div>
            </div>
        </li>

        <!--on show when choose vendor-->
        <div id="choose-vendor">
            <li class="fields">
                <div class="field">
                    <label for="file">
                        Logo
                    </label>

                    <div class="input-box">
                        <input type="file" name="logo" class="input-text"/>
                    </div>
                </div>

                <div class="field">
                    <label for="type" class="required"><em>*</em>
                        Paypal Account
                    </label>

                    <div class="input-box">
                        <input type="text" name="paypal_account" value="" class="input-text required-entry">
                    </div>
                </div>
            </li>
        </div>
        <!-- //end on show when choose vendor-->
    </ul>
</div>
<!-- select choose customer change -->
<script>
    jQuery(document).ready(function () {
        jQuery('#form-validate').attr('enctype',"multipart/form-data");
        $html_vendor = jQuery('#choose-vendor').html();
        jQuery('#choose-vendor').html('');

        jQuery('#select-customer-type').change(function(){
            $type = jQuery(this).val();
            if($type == 'vendor') {
                jQuery('#choose-vendor').html($html_vendor);
            }
            else {
                jQuery('#choose-vendor').html('');
            }
        });
    });
</script>